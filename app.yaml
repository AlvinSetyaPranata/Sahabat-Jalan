runtime: python312

entrypoint: gunicorn -b :$PORT server.server.server.wsgi:application

env_variables:
  DJANGO_SETTINGS_MODULE: "server.server.server.settings"


handlers:
- url: /static
  static_dir: static/

- url: /.*
  script: auto

